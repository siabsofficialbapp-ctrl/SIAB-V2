# Railway's builder runs with NODE_ENV=production, which would otherwise skip
# devDependencies — esbuild lives there and the build needs it.
prod=false
# Hoist enough for esbuild's platform binary to resolve on the builder.
node-linker=hoisted
